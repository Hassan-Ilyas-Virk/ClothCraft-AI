streamlit==1.36.0
numpy>=1.24,<3
Pillow>=10.0.0,<11
opencv-python>=4.9.0
scikit-image>=0.22
click>=8.1
tqdm>=4.66

# Torch stack: installed separately — see README for CPU-only install command
# pip install torch==2.3.1 torchvision==0.18.1 torchaudio==2.3.1 --index-url https://download.pytorch.org/whl/cpu

# Perceptual similarity for projection
lpips==0.1.4 